# Active Session Role

The active AI session (Claude Code or Codex) implements for that session.
Human switches between them as needed — they do not run simultaneously.
Human approval required for commits, pushes, merges, tags, and deploys.
See `.ai/00-master-control.md` for the full authority model.
